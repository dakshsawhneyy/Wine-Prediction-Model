pip install dvc
pip install "dvc[azure]"

dvc init
git commit -m "Initialize DVC"

cd data/
dvc add wine.csv

dvc remote add -d myremote azure://winepredictiondvc/data
dvc remote modify myremote account_name "winepredictiondvc"

